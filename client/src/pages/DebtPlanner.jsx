import ComingSoon from '../components/ComingSoon.jsx';

export default function DebtPlanner() {
  return <ComingSoon page="Debt Planner" sprint="4d (debt list + strategy + What-If BT + forecast chart)" />;
}
